name: Daily Top News

on:
  schedule:
    # Runs every day at 06:00 UTC. Adjust as needed.
    - cron: '0 6 * * *'
  workflow_dispatch: {}   # allows manual trigger from the Actions tab

permissions:
  contents: write   # needed so the workflow can commit README.md back to the repo

jobs:
  update-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch top 5 news
        run: python fetch_news.py

      - name: Commit and push if README.md changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "Update top news $(date -u +'%Y-%m-%d')"
          git push


